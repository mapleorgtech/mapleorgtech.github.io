on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: whatsapp-notify
        id: whatsapp-notify
        env:
          account_sid: ${{ AC27b3abb7421206e10413d9f8056a3411 }}
          auth_token: ${{ 3bd9c32e4693bee7ad99965f0c8f98cd }}
          to_whatsapp_no: ${{ +917898164077 }}


        uses: ishween/whatsapp-push-notify-action@master
      
      - name : Run
        run: |
          echo 'Start!' 
